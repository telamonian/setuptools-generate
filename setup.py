#!/usr/bin/env python
from setuptools import setup

setup(
    author = 'Max Klein',
    description = "Some extensions for your setup.py scripts that make it easy "
                  "to incorporate the generation of python source code (by an " 
                  "external tool such as `protoc`) into the setup of your modules",
    license = 'BSD',
    name = 'setuptools_generate',
)
